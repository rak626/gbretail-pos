NEXT_PUBLIC_API_URL=http://localhost:4000
# DATABASE_URL is now only in gbretail-backend/.env — frontend does not need it
# Set to 1 only for local demos to show test credentials on /login (never in production)
# NEXT_PUBLIC_SHOW_DEMO_CREDS=1
