# Frontend — POS (no DB direct, calls backend)
# Local: http://localhost:4000 | Prod: https://gbretail-backend.<workers>.dev
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Backend now owns DATABASE_URL — see gbretail-backend/.env.example
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gbretail?schema=public"
